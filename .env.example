# 複製為 .env（不進版控）
# Ollama 位址：本機預設 11434；將來可指向 LAN 上的另一台機器（如 Mac mini）
OLLAMA_BASE_URL=http://localhost:11434

# 可切換的 Ollama 端點清單（選填）——UI 頂部的端點切換器列這些。
# 格式：`標籤|網址`，多個以逗號分隔；OLLAMA_BASE_URL 一定會在列（不必重複）。
# 網址只在這裡（伺服器端）定義，前端只能「選」不能指定任意網址（防 SSRF）。
# 範例：本機 demo 與 LAN 上的 Mac mini 兩個端點：
# OLLAMA_ENDPOINTS=本機 demo|http://localhost:11434,LAN Mac mini|http://192.168.0.17:11434

PORT=3000
